# Frontend env. Copy to .env.local for dev. NEVER commit .env.local.
# In production (served by Caddy on the same origin), leave empty so the
# fetch wrapper uses relative URLs.
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_RP_ID=anvisutra.com
NEXT_PUBLIC_PUBLIC_ORIGIN=https://anvisutra.com
